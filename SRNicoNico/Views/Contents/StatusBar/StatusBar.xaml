<UserControl x:Class="SRNicoNico.Views.StatusBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:SRNicoNico.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="20" d:DesignWidth="305"
			 d:DataContext="{d:DesignInstance vm:StatusBarViewModel}">
    <Grid>
		<TextBlock Text="{Binding Status}" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0,0,0" />
        <TextBlock Text="{Binding Time}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0" />
    </Grid>
</UserControl>
