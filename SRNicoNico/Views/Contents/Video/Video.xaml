<UserControl x:Class="SRNicoNico.Views.Contents.Video.Video"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Behaviors="clr-namespace:SRNicoNico.Views.Behaviors"
             xmlns:Wpf="clr-namespace:xZune.Vlc.Wpf;assembly=xZune.VLC.WPF"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:l="http://schemas.livet-mvvm.net/2011/wpf"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:SRNicoNico.ViewModels"
             d:DesignHeight="300"
             d:DesignWidth="300"
			 d:DataContext="{d:DesignInstance {x:Type vm:VideoViewModel}}"
             mc:Ignorable="d"
			 DataContext="{Binding Video}">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition />
			<RowDefinition />
		</Grid.RowDefinitions>

		<Wpf:VlcPlayer x:Name="VlcPlayer" Grid.Row="0" Background="Black">

			<i:Interaction.Behaviors>
				<Behaviors:VlcBehavior />
			</i:Interaction.Behaviors>
		</Wpf:VlcPlayer>
		<TextBlock Grid.Row="1" Width="20" Height="20" Text="{Binding Position, ElementName=VlcPlayer}"/>
		<Slider Grid.Row="0" Height="30" VerticalAlignment="Bottom" Value="{Binding Position, ElementName=VlcPlayer}" Maximum="1" Minimum="0" SmallChange="0.0000001" LargeChange="0.000001"/>


	</Grid>

	<i:Interaction.Triggers>

		<!--  Windowが閉じたタイミングでViewModelのDisposeメソッドが呼ばれます  -->
		<i:EventTrigger EventName="Unloaded">
			<l:DataContextDisposeAction />
		</i:EventTrigger>
	</i:Interaction.Triggers>


</UserControl>
