<UserControl x:Class="SRNicoNico.Views.Contents.Video.Video"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mui="http://firstfloorsoftware.com/ModernUI"
             xmlns:Wpf="clr-namespace:xZune.Vlc.Wpf;assembly=xZune.VLC.WPF"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:l="http://schemas.livet-mvvm.net/2011/wpf"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:SRNicoNico.ViewModels"
             xmlns:video="clr-namespace:SRNicoNico.Views.Contents.Video"
             d:DesignHeight="300"
             d:DesignWidth="300"
			 d:DataContext="{d:DesignInstance {x:Type vm:VideoViewModel}}"
             mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition Height="*"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <video:VideoInternal Grid.Row="1" />

        <mui:ModernButton Grid.Row="2" VerticalAlignment="Top" HorizontalAlignment="Left" IconData="M 10,0 v 20 l 20,-10 Z" >
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="Click">
                    <l:LivetCallMethodAction MethodName="Play" MethodTarget="{Binding}" />
                </i:EventTrigger>
            </i:Interaction.Triggers>

        </mui:ModernButton>
        <TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" Text="{Binding }" VerticalAlignment="Top"/>


    </Grid>

    <i:Interaction.Triggers>

        <!--  Windowが閉じたタイミングでViewModelのDisposeメソッドが呼ばれます  -->
        <i:EventTrigger EventName="Unloaded">
            <l:DataContextDisposeAction />
        </i:EventTrigger>
    </i:Interaction.Triggers>


</UserControl>
