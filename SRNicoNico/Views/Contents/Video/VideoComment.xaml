<UserControl x:Class="SRNicoNico.Views.VideoComment"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:l="http://schemas.livet-mvvm.net/2011/wpf"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:metro="http://schemes.grabacr.net/winfx/2014/controls"
             xmlns:vm="clr-namespace:SRNicoNico.ViewModels"
             xmlns:controls="clr-namespace:SRNicoNico.Views.Controls"
             d:DataContext="{d:DesignInstance Type=vm:VideoCommentViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="250">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid Background="{DynamicResource BackgroundBrushKey}" Height="28">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>
            <ComboBox SelectedItem="{Binding SelectedThread, Mode=TwoWay}" ItemsSource="{Binding CommentThreads}" VerticalContentAlignment="Center">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Label, Converter={StaticResource CommentLabelConverter}}" FontSize="16" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <controls:RoundButton ToolTip="オートスクロール" IconData="{StaticResource ReloadIcon}" Margin="5,0">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <l:LivetCallMethodAction MethodTarget="{Binding}" MethodName="Reload" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </controls:RoundButton>
                <controls:RoundButton ToolTip="コメントを更新" IconData="{StaticResource ReloadIcon}" Margin="0,0,5,0">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <l:LivetCallMethodAction MethodTarget="{Binding}" MethodName="Close" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </controls:RoundButton>
            </StackPanel>
        </Grid>
        <ListBox Grid.Row="1" ItemsSource="{Binding SelectedThread.Entries}" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Background="Transparent">
                        <Grid.ContextMenu>
                            <ContextMenu>
                                <MenuItem />
                                <MenuItem />
                            </ContextMenu>
                        </Grid.ContextMenu>
                        <Grid.ToolTip>
                            <TextBlock>
                                <Run Text="{Binding Number, StringFormat={}{0:N0}}" />
                                <Run Text="{Binding DateTime, StringFormat='yyyy年M月d日 HH:mm:ss'}" />
                                <LineBreak />
                                <Run Text="{Binding Content}" />
                                <LineBreak />
                                <Run Text="{Binding Mail}" />
                            </TextBlock>
                        </Grid.ToolTip>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto" />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Vpos, Converter={StaticResource VposDurationConverter}}" Margin="5,0" />
                        <TextBlock Grid.Column="1" Text="{Binding Content}" TextTrimming="CharacterEllipsis" Height="16"  />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <metro:ProgressRing Grid.Row="1" IsActive="{Binding IsActive}" Panel.ZIndex="1" />
    </Grid>
</UserControl>
