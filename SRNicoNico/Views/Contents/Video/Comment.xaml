<UserControl x:Class="SRNicoNico.Views.Contents.Video.Comment"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:vm="clr-namespace:SRNicoNico.ViewModels"
             xmlns:local="clr-namespace:SRNicoNico.Views.Contents.Video"
			 xmlns:comment="clr-namespace:SRNicoNico.Views.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="204.918" d:DesignWidth="373.77"
			 d:DataContext="{d:DesignInstance {x:Type vm:VideoViewModel}}"
			 >

    <Grid x:Name="view">
        <comment:CommentView CommentList="{Binding VideoData.CommentData}" DrawingGrid="{Binding ElementName=view}" CurrentTime="{Binding Time.CurrentTimeMilis}" IsPlaying="{Binding IsPlaying}" IsSeekBarChanged="{Binding IsSeekChanged, Mode=TwoWay}" CommentVisibility="{Binding CommentVisibility}" />
		
			
			
			
    </Grid>

</UserControl>
