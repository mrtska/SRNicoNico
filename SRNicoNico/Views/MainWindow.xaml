<mui:ModernWindow x:Class="SRNicoNico.Views.MainWindow"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:contents="clr-namespace:SRNicoNico.Views.Contents"
                  xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions"
				  	   xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                  xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
                  xmlns:l="http://schemas.livet-mvvm.net/2011/wpf"
                  xmlns:mui="http://firstfloorsoftware.com/ModernUI"
                  xmlns:search="clr-namespace:SRNicoNico.Views.Contents.Search"
                  xmlns:nicorepo="clr-namespace:SRNicoNico.Views.Contents.NicoRepo"
                  xmlns:v="clr-namespace:SRNicoNico.Views"
                  xmlns:vm="clr-namespace:SRNicoNico.ViewModels"
				  xmlns:local="clr-namespace:SRNicoNico.Views.Controls"
                  xmlns:root="clr-namespace:SRNicoNico.Views.Contents"
                  Title="{Binding Title}"
                  Width="870"
                  Height="723"
                  MinWidth="600"
                  MinHeight="500"
                  HorizontalAlignment="Stretch"
                  Background="#FF2D2D2D"
                  FontFamily="Meiryo"
                  FontStyle="Normal"
                  FontWeight="Normal"
                  Style="{StaticResource BlankWindow}"
                  WindowStyle="None"
                  x:Name="Root"
				  >



    <i:Interaction.Triggers>
        <l:InteractionMessageTrigger Messenger="{Binding Messenger}">
            <l:TransitionInteractionMessageAction/>
        </l:InteractionMessageTrigger>


        <!--  Viewに特別な要件が存在しない限りは、トリガーやアクションの自作にこだわらず積極的にコードビハインドを使いましょう  -->
        <!--  Viewのコードビハインドは、基本的にView内で完結するロジックとViewModelからのイベントの受信(専用リスナを使用する)に限るとトラブルが少なくなります  -->
        <!--  Livet1.1からはコードビハインドでViewModelのイベントを受信するためのWeakEventLisnterサポートが追加されています  -->

        <!--  WindowのContentRenderedイベントのタイミングでViewModelのInitializeメソッドが呼ばれます  -->
        <i:EventTrigger EventName="ContentRendered">
            <l:LivetCallMethodAction MethodName="Initialize" MethodTarget="{Binding}" />
        </i:EventTrigger>

        <!--  Windowが閉じたタイミングでViewModelのDisposeメソッドが呼ばれます  -->
        <i:EventTrigger EventName="Closed">
            <l:DataContextDisposeAction />
        </i:EventTrigger>

        <!--  WindowのCloseキャンセル処理に対応する場合は、WindowCloseCancelBehaviorの使用を検討してください  -->

    </i:Interaction.Triggers>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="215"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>


        <local:TabControlEx Grid.Column="0" Background="Transparent" BorderThickness="0" TabStripPlacement="Left" >


            <local:TabItemEx Text="検索">

                <search:Search />
            </local:TabItemEx>

            <local:TabItemEx Text="二コレポ">

                <nicorepo:NicoRepo />
                
            </local:TabItemEx>
            <local:TabItemEx Text="マイリスト">
                
                
            </local:TabItemEx>


        </local:TabControlEx>

        <Line X1="3" X2="3" Grid.Column="1" Y1="0" Y2="{Binding ElementName=Root,Path=ActualHeight}" Stroke="#FF333333"/>

        <contents:Start Grid.Column="2" Grid.ColumnSpan="2" />
    </Grid>
</mui:ModernWindow>
